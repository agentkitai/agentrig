# R16h — fixed themes and bounded keybindings

New branch from green main8fe6f29 (#194, all-four CI34091238822 /
structure34091238830). R16g integration proceeds independently; merge it first,
then integrate and retest this row. One PR and one bounded Claude review.

Contract before implementation: optional strict `tui` configuration selects
`theme: dark | light` and a small `keybindings` table. Permission actions
`allowOnce`, `denyOnce`, `allowSession`, `denySession`, `scope`, `editScope` accept
one ASCII letter, unique ignoring case after filling defaults. Scope confirmation
uses allowOnce; cancellation uses denyOnce. Escape remains deny/cancel and Ctrl+C
remains abort/exit. History previous/next accept up/down/ctrl-p/ctrl-n with distinct
bindings. Optional abort alias is ctrl-g only. No runtime editor, arbitrary
terminal sequences, question/supervisor rebinding or user-defined ANSI colours.

Validate before provider construction or Ink mount, preserve trusted config
resolution, and pass immutable settings through actual startup. Both ordinary
and protocol-replay input use the same bindings only after bracketed-paste
decoding. Preserve scope-preview identity, coalesced confirmation refusal,
protected input exclusion, paste bytes and existing command ownership.

Two fixed palettes supply the five transcript tones plus prompt, warning and
status colours. NO_COLOR suppresses all added TUI SGR styling, including Markdown
and captured diff output, even with FORCE_COLOR set; no global/headless colour
mutation. Preserve Static scrollback and existing width/height bounds.

Acceptance: strict config and default-collision negatives, actual startup/Ink
light/dark frames at80/120 columns, remapped permission/scope/history/abort in both
input paths, protected prompts/pasted controls unchanged, NO_COLOR raw-output
checks, fail-before/restored wiring and mapping controls. Build/typecheck,
required-Docker full suite, real Chromium, exact-head and post-main green CI.
Minor polish stays at roadmap END; no new milestone hierarchy.

## Implementation evidence

Config resolution reaches actual startup and real Ink in both ordinary and
protocol-adjacent input. Startup captures a validated copy before asynchronous
construction; mutating the caller's configuration afterward does not change the
effective answer key. Invalid direct settings refuse before provider construction
or mounting. Existing whole-object config precedence is retained.

Removing only the startup `settings` prop caused both retained integration cases
to fail (1046/1018ms): prompts showed default y, not configured j. Restoring the
wiring passes all five startup controls. Build/typecheck pass; the initial
settings/config/startup group passed66 tests. Initial test failures were fixture
setup mistakes (user state nested inside the project and omitted budget defaults),
corrected without changing trust or budget enforcement. Final counts follow.

Controller prose no longer hard-codes default permission keys; the live prompt
names the effective keys. ASCII-only key matching also excludes Unicode casefold
lookalikes. NO_COLOR tests must start a fresh process with FORCE_COLOR set before
Ink/Chalk imports; changing it after imports is not positive colour evidence.

Frozen implementation1cea9f8: build/typecheck passed;90 focused tests passed.
Removing shared action mapping failed both actual history/abort input paths;
forcing the default palette failed both fresh-process light frames at80/120.
Restored source passes, including dark/light/NO_COLOR positive and negative controls.
Required real digest-pinned Docker full suite:3,194 passed plus two existing skips
/206 files,80.96s. Real Chromium:one passed,2.84s.

[Independent review](R16h-review.md): one substantive review,26 total reported
turns including the sole tools-disabled summary; static APPROVE, no reviewer tests.
No material findings; indentation corrected and optional polish at roadmap END.
Final R16g integration, exact-head CI and post-main receipts remain pending.

## Integrated delivery checkpoint

Integrated R16g PR#210 main `98eb403989d7e7e14747a0ea7c2943f4f1b72c43`.
Unified key dispatch preserves `!controller.isIdle()` ownership, so both fixed
Ctrl+C and opted-in Ctrl+G abort actual held compaction in ordinary/protocol paths
without exiting or adopting a child. Four actual-maintenance controls pass.
Build/typecheck and49 combined focused tests pass. Final source0297ed2 required
digest-pinned real-Docker suite:3,242 passed plus two existing skips /209 files,
80.32s. Real Chromium:one passed,2.53s. No second broad review for integration.
Read-only documentation check found241 local targets resolving; the sequencing
fragment exists. Exact-head and post-main hosted delivery gates follow.
