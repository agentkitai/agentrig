# Nested skill filename casing

Existing R5c END follow-up, independently assigned from green main `300c5e2`.
Package validation accepts case-insensitive nested markers while core discovery
constructs exactly `<name>/SKILL.md`. On case-sensitive filesystems the accepted
case variant disappears from discovery; portable packages must use exact spelling.

Refuse nested `skill.md` case variants with an explicit `SKILL.md` diagnostic,
before destination publication or installed-package loading. Preserve flat `.md`
case-insensitivity, canonical nested markers, ordinary resource files, existing
trust/discovery switches, path/link/count/byte validation and integrity checks.
No loader broadening, case normalization, migration, new schema/event or new
milestone. The separate hardlink-source documentation follow-up remains unchanged.

Prove directory/archive refusal before publication and actual trusted package
install→builder→core discovery with canonical nested and uppercase flat files.
Fail-before/restored controls, one bounded Claude review with exact evidence limits,
build/typecheck, digest-pinned required Docker full suite and real Chromium are
required. Root serializes ready-item merges; no live provider or OS clipboard work.

## Initial controls

Six fail-before refusal controls resolved instead of rejecting: four directory
marker variants, one archive, and trusted-runtime negative setup. The canonical
nested/uppercase-flat install→actual builder→core load positive already passed.
With the shared four-line guard restored, all 70 package, runtime and core-skill
tests pass. A real installed-file spelling change (via intermediate rename for
case-insensitive hosts) is refused at inspection with the explicit same diagnostic,
preserving the changed files. This does not claim authenticity or migrate legacy
packages automatically. No core-loader broadening or filename normalization.
