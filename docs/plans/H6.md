# H6 — focused core extraction

Fresh branch `refactor/h6-core-components` from main `3c09f84` after R4c PR #137 exact-head
CI 34010906102 and post-merge CI 34011105236 passed all three platforms.

Extract cohesive tool execution (validation, permissions, sandbox, checkpoints, output shaping
and emission authority) and session lifecycle (event sequencing, pause/abort, orphan settlement,
terminal close) from agent.ts. Keep the public exports, model loop and hook semantics unchanged.
Internal typed dependencies only; no plugin framework, parallel execution or new behavior.

Capture deterministic complete event/request/snapshot traces on the pre-extraction implementation
before moving code. Preserve them without updating expected values after extraction. Existing
permission, sandbox, hook, abort, resume, subagent and checkpoint suites remain required.
Mutate event order to prove the baseline detects a regression, then restore. One bounded
independent review, exact-head all-platform CI, merge, and post-merge main CI precede R6d.
Optional polish belongs at ROADMAP's end, not in additional H6 subdivisions.

Three full golden traces were generated while agent.ts was still identical to main `3c09f84`:
permission/hook/tool/resume; aborted-tool settlement and late-emission guard; emitter authority
and replan release. Fixed clock/id/synthetic cwd remove host variability without omitting fields.
Baseline SHA256: `cb5f909d12cedfd1766bd0f11165faeb0815695a5dd4315cf939130bab108922`.
After extraction, all three unchanged snapshots plus 78 agent tests pass; build/typecheck pass.

## Validation receipt

Full Node22 suite: 1,934 passes plus two skips (93 files). One independent Claude review
`5a8574a4-1a94-4e7c-92cf-39dade4b1b47` approved without material findings, passing build/typecheck
and 372 cases across 17 suites. Existing suites cover sandbox, checkpoints, overflow, pause,
orphan grace and authorized replan clearing beyond the three golden traces.

Swapping permission request/decision order fails the unchanged sequential snapshot. Leaving the
terminal ended flag false initially passed the replay-only test: its replay could race a queued
late append. Added a direct append-spy assertion after one microtask; it detects the mutation.
Both mutations are restored. The complete trace suite, including the stronger assertion, was
also rerun successfully against the exact original main agent.ts (diff against main empty), then
the extraction was restored. Snapshot expectations were never regenerated after extraction.
Removed an unused import. These are maintainer validation refinements, not another review round.
Exact-head PR and post-merge main CI remain required before R6d.
