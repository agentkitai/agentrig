# R17g supervisor follow-ups

Bounded package sweep from green main4c82ebb. No live evaluation, frozen E3/R17f
retuning, authority upgrades or new review gates. Supervisor imports core types only.

| Existing fragment | Disposition / acceptance |
|---|---|
| Exact repeated partial-ledger warning | Actual emitter is CLI run.ts attempts callback, not reviewer.ts. Root CLI batch owns per-supervise exact corruption-list dedupe, healthy reset and changed-history controls. No generic supervisor API added. |
| Auxiliary provisional state | Implemented optional core call.state running/settled, supervisor model and opaque custom paths. Memory batch mirrors the state and owns neutral formatAuxiliaryUsage rendering (CLI consumes that formatter). Legacy outcome remains unchanged; in-flight and missing usage remain unknown. |
| Undefined partial auxiliary limits | Declined compatibility change: explicit undefined remains invalid rather than silently changing configured limits; existing tests pin pre-work validation. |
| Absolute/relative drift paths | Implemented cwd-aware matching using recorded alias and verified canonical root; POSIX/Windows and real-file controls. Outside-root and directory-boundary refusals persist; slash-only legacy whole-task marker stays task-bound, not host-root permission. No physical attestation for plan paths. |
| Destructured detector observe | Implemented receiver-independent loop/drift recursion; real corroborated-result fixture exercises both methods as unbound callbacks. |
| Failed pending calls | Implemented early failed legacy/denied ID-matched pending-write release. Late correlated success cannot revive discarded claims. Proper attributed results already released their pending entry; retained. |
| Fold isolation | Implemented independent state/evidence boundaries and collector recovery. Lost folds retain explicit incomplete report; later useful plans survive. Existing grader incomplete-view controls remain fail-closed. |
| Original evaluator attestations | Implemented original verdict/completeness beside derived lane assessment; same-assumption PASS→BLOCKED control retains both truths. No lane verdict or custody logic changes. |
| Malformed verification loader | Keep existing error/no-grade path: it already fails closed and reports auxiliary error. A fabricated replacement report would obscure invalid trusted input; no second evaluator. |
| Allocation/index optimizations | Declined without workload measurements; current scans/maps have explicit caps. No claim that unchanged allocation is optimal. |
| Historical deletion witnesses | Declined inference from current absence: requires actual prior witness, not additional heuristics. |
| Historical plan seeding | Declined scope expansion: attached report explicitly covers current run, physical-log CLI separately covers supplied history; preserve both disclosures. |
| Longer-lived evidence fixture cleanup | Existing demonstrated failures repaired (#206/#208); no new long-lived children in this package change, so no speculative fixture restructuring. |

Validation uses a private mktemp TMPDIR and fixture preflight; no marker bypass or
foreign cleanup. Fail-first controls and restored mutations, package-focused tests,
build/typecheck. Root owns combined full gates, one bounded independent review and PR.

## Validation receipt

- Private TMPDIR `/var/tmp/agentrig-r17g-supervisor-tests.P7LGI2`; preflight passed
  with lexical/canonical ancestor checks and no marker bypass/deletion.
- Initial new test run: six meaningful failures (scope spelling, destructured
  result recursion, denied/legacy-failed pending release, original attestation,
  provisional state). Two other initial failures were fixture setup defects
  (missing session.start model), corrected and not counted as defect evidence.
- Corrected fold controls then independently killed two named mutants: skip
  evidence.observe when reduce throws; rethrow collector fold errors. Both failed
  their exact later-plan/no-throw assertions, then source restored. Log:
  `/var/tmp/r17g-supervisor-fold-mutants.log`.
- Restored full supervisor plus core events: 314 passed across15 files, 7.18s;
  build and typecheck passed. Includes actual file verification/canonical alias,
  actual attach→grader input, schema roundtrip/legacy/invalid-state controls,
  opaque custom calls and successful/failed auxiliary call lifecycle tests.
- No independent review initiated by package agent. Root owns review, combined
  cross-package validation, full Docker/Chromium and exact-head hosted delivery.

## Bounded review repair

Claude `54ba8e26-dfae-4ee8-ba92-89cea94c0f8d` found a diagnostic regression: the
collector retained incomplete evidence but swallowed the exception before the
host error callback could observe it. An isolated optional collector callback now
reports that failure; the unreachable outer catch is removed. The real attach
regression failed first with zero evidence notifications, then checks one callback,
a throwing host diagnostic, retained later plan and explicit `fold errors 1`.
Neither a failed fold nor a failed diagnostic can certify complete evidence.
Codex `01a0877b-4f0f-7622-85b2-a6f806600965` found no substantive issue in the first pass.

Other review observations do not open new gates: with a known cwd, `/src` is an
absolute host path, not legacy shorthand for `src`; use relative `src` or the full
task-root path. Slash-only `/` retains its documented task marker. A state-fold
failure conservatively increments omitted-event coverage even when the independent
evidence fold retains that event; the incomplete label is intentional. Memory's
separate batch supplies running-state rendering. No historical scores or authority
boundaries changed, and no general review is restarted for these observations.
