# R17d — permission friction

Builder: **agentrig**, conductor **96c179ab**, continuation of conductor **a4decdf9** / builder **b97650ae** on PR #273. No child/reviewer spawning, E3 spending, provider/permission settings changes or later-row implementation.

## Deviations — human-resolved contract correction

Historical arbiter **a769b04f** remains **REJECT/needs-human**, never APPROVE.
[Halt receipt](https://github.com/agentkitai/agentrig/pull/273#issuecomment-5584225088).
The human resolved [feel #274](https://github.com/agentkitai/agentrig/issues/274) outside the train:

Assistant proposed:
"Preserve the historical E1 benchmark as an unchanged control. Before implementation, freeze supplemental E1 traces exercising bash grants and repeated requests. Require at least one fewer prompt per task afterward, with identical traces and response policy, unchanged authority, and all R13e security fixtures passing."
Assistant asked:
"Approve this correction so I can resume #273, then proceed through R17e → R17f → R17g with reviews and sequential green-CI merges?"
Human replied:
"approved"

That exact acceptance is now in ROADMAP. The three product requirements and security renunciation are unchanged. No other gate is waived.

## Frozen benchmark (before product changes)

`R17d-supplemental.mjs` copies the historical deterministic E1 reference trace and appends two identical `git status --short` foreground bash calls in each task's actual prepared fixture workspace. Uses real core dispatch and the TUI approval controller, no paid model. Task edits, checks, prompts and fixture revisions remain the historical ones. Historical `.agentrig/r17/task-baseline.mjs`, CLI feel limits and reference are unchanged controls.

Frozen response policy: explicitly confirm an offered default scoped grant, otherwise allow once. Never select allow-all; no silent approval or automatic conversion of one-time consent. The script uses the same feature-detection branch before and after product implementation. Target: at least **one fewer permission prompt per task** on these identical traces/policy. A4/X4 automated behavior assertions are not a complete manual pass: outcome remains **BLOCKED/unknown**.

Read-class inside trusted root already auto-allows under R17b defaultRules. Preserve that and prove outside-root, explicit-deny and R13e boundaries rather than replacing it with a broader tool-name allowlist. Bash default must offer the full literal argv plus exact cwd with explicit preview/confirmation; shell syntax, background, separate consent and missing runtime grants cannot gain standing authority. Repeated queued asks must consume an already explicitly confirmed covering scoped decision; allow-once remains once.

## Result and security bounds

The freeze is commit **770df83**, after merge **82f3435** and before any product changes. `R17d-before.json` and `R17d-after.json` contain full task measurements, evaluator outcomes and trace SHA-256. The frozen runner is byte-identical before/after. Temporary fixture roots and generated IDs vary, not trace operations or response policy.

| Task | Supplemental before | Supplemental after | Saved | Unchanged historical control after |
|---|---:|---:|---:|---:|
| A1 | 4 | 3 | 1 | 2 |
| A2 | 4 | 3 | 1 | 2 |
| A3 | 5 | 4 | 1 | 3 |
| A4 | 4 | 3 | 1 | 2 |
| X1 | 4 | 3 | 1 | 2 |
| X2 | 4 | 3 | 1 | 2 |
| X3 | 4 | 3 | 1 | 2 |
| X4 | 4 | 3 | 1 | 2 |

Historical control reproduces its prior counts. Supplemental total **33 → 25**, saving exactly **one prompt/task**. All automated task checks retain the historical result; A4/X4 **BLOCKED/manual PENDING**, not full manual PASS. This scripted, fixture-relative E1 supplement is not a paid-model/live-user evaluation, not E3, and not a substitute for the manual gate.

The product change uses existing R12b grants, not a new allowlist or approval cache. Enter previews the full literal argv and exact cwd; a separate displayed confirmation key installs the existing session grant. Prefix matching still permits trailing argv and existing descendant delegation, both disclosed by the existing preview. No shell-syntax, background, outside-sandbox, MCP-change or external-input-expansion consent is transformed into this default. No claim that git command names or reads are inherently safe. An explicit base deny and core pre-dispatch/audit rechecks still win. Read-class trusted-root defaults are unchanged and regression-tested. Repeated queued requests reconsult their own live registry after a scoped decision; unscoped allow-once remains once, uncovered requests still ask, and expired/restricted child context remains fenced.

## Verification and accounting

- Fail-first: initial corrected controller suite exited **1** before product edits, including missing default offer and queued duplicate remaining pending. Early fixture-shape mistakes were corrected before that run; they are not product regressions.
- Builder load-bearing mutation probes: remove only the Enter binding → dedicated real Ink test exits **1**; remove only queued-registry recheck → duplicate regression exits **1**. Mutations restored, never committed.
- Focused R12b/R17d/R13e tests: **38 passed**, exit **0**; expanded permission/core/real-frame selection **40 passed**, exit **0**.
- Local trio: `pnpm build` **0**, `pnpm test` **0** (**222 files; 3458 passed, 4 skipped**), `pnpm typecheck` **0**. Supplemental before/after and unchanged historical control runners each exit **0**. Exact-head CI receipt follows in the PR.
- Builder is **agentrig**, conductor **96c179ab**. Parent records the true spawned child/session ID and authoritative tokens; neither is exposed in this task context, so not invented. This child spawned **zero** children/auxiliary reviewers, made **zero E3** calls and no provider-setting changes. No new genuine harness friction was identified; fixture test mistakes and a bad path lookup are ordinary work, not feels. No budget draw from the **12M reserved for R17f**. Stop at PR; independent parent review and human-controlled landing remain outstanding.
- Predecessor and outside recovery attribution remains in STATUS. Feel250 was closed after actual omission/route evidence from child1454d69b; #275 and #272 and PR276 LOW residuals #277/#278/#279 are END followups, not extra R17d gates.

## Review repair and operator handoff

The implementation was built by agentrig (conductor96c179ab, child1454d69b).
Review conductor11dfb0d6 and repair child75fb2099 were stopped by the operator
after repeated workflow overhead; Codex/operator completed the repair outside
the train, preserving the existing branch and tests. The human explicitly
directed end-to-end completion including independent reviews, CI and merge.

The [first Claude review](https://github.com/agentkitai/agentrig/pull/273#issuecomment-5586644560)
found a vacuous queued-child test and missing consumption accounting/audit gating.
The new child test actually queues an allowed bash tool behind its parent and
proves a non-delegable grant does not cross that boundary. Queued consumption
uses `authorize`, not preview-only `decide`; a same-call core callback durably
flushes audit events first. Failed/missing audit retains the prompt, while an
aborted/replaced request cannot consume authority after the flush. Tests cover
consumed counts, failed append, cancellation, and the real core callback's
successful/failed durable append. Core's existing post-answer flush remains in
place: the original preview-path issue is not a claim that dispatch lacked that
later audit check.

Operator mutation checks independently killed: removing the audit callback's
flush (both real-dispatch cases fail), switching queued authorization to the
parent registry (child-confinement case fails), and removing the Enter binding
(real Ink case fails). Every run was joined and the original source restored.
The permission/controller and core injection fixtures are explicitly included
in the Windows suite. R13e fixture source and its negative assertions remain
unchanged; a passing assertion suite is not a waiver of those checks.

The frozen supplemental runner was rerun after repair, unchanged: prompts
3/3/4/3/3/3/3/3 (25 total), still one fewer per task than the pre-product freeze.
The unchanged historical control rerun remains 2/2/3/2/2/2/2/2. Both runners
exited0; A4/X4 remain manual BLOCKED, with automated behavior PASS. Operator
artifacts: `/tmp/r17-e1-mkgkWf/results.json` and `/tmp/r17-e1-lDdeLW/results.json`.
Reproduce with `pnpm build`, `node docs/plans/R17d-supplemental.mjs`, and the
unchanged historical `.agentrig/r17/task-baseline.mjs` control (where available).
The committed original before/after evidence and frozen runner are not rewritten.

The review's acceptance-provenance question is resolved by the existing explicit
human approval, transcribed in the
[operator clarification](https://github.com/agentkitai/agentrig/pull/273#issuecomment-5586632347),
not by inventing an arbiter APPROVE. The approved acceptance quote above and the
original security renunciation remain unchanged. The initial Claude review did
not run the full suite; that limitation is retained and final independent delta
verification is required before merge.
