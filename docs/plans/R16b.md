# R16b — bounded captured transcript diffs

## Contract before implementation

One row and one PR from green main `74699dac9fd6a1914e741e5f102cdb36d4bcfb04`.
This can run alongside R16a/c/d: existing Static ANSI rendering and R12b's
permission panel suffice; there is no dependency on the Markdown parser.
Final integration preserves the tool-summary renderer and all permission receipts.

Successful actual built-in edit/write operations attach captured before/after
observations to their canonical result events. A private factory identity and
result/context receipt prevent a same-named extension or JSON result field from
claiming a builtin observation. Capture occurs inside authorized execution;
rendering never reads files or invokes Git. Diagnostics and post-tool display
rewrites do not replace the original captured observation. Model messages and
metadata-only telemetry do not acquire this content; canonical raw logs are
sensitive and do retain it. This is an observation, not a filesystem transaction,
writer-isolation proof, or assurance about later disk state.

Capture retains at most 64 KiB UTF-8 and 6,000 lines per side; paths at most 4,096
characters. Truncation is explicit. Existing edit-file reads are unchanged; the
additional write-file before read uses a bounded regular-file handle and refuses
symlinks/nonregular files for observation, without changing existing write
authorization. While an enforcing sandbox is active the additional host-side
write-before read is skipped and reported unknown; no new read bypass is added.
Missing before is empty only for an initial observed ENOENT; unreadable,
binary, unsupported, or raced observations remain unknown, never invented
deletions. After comes from the exact successful write input, not a reread.

Permission asks use the same renderer with a different explicit scope: edit input
is a proposed replacement excerpt (not whole-file before/after); write input is
proposed content with unknown prior contents. Preview performs no filesystem
reads and cannot grant permission. Only actual builtin identity derives it from
validated input; declared effects remain alongside it. Denial has no write or
before-read effects.

Use pinned jsdiff structuredPatch with context 3 and maxEditLength 2,000 over
bounded captured strings. Aborted/high-complexity comparisons render labelled
before/after excerpts, not a purported minimal patch. Output has at most 80 lines
per file (including explicit elision), 512 printable characters per line and
48 KiB overall. Unknown sides and truncated snapshots are visibly labelled.
Added/removed/context and hunk headers use only fixed application ANSI codes;
source terminal controls and bidi controls are sanitized, NO_COLOR respected.
No applying patches, new diff framework, editable views, or wrapping outside the
existing viewport. Bounds do not claim hard CPU/RSS containment.

## Required controls and delivery

- Actual edit/write sessions: captured before/after, CRLF, create, unknown before,
  5,000-line changed file with capped output and elision; later external edits do
  not alter event rendering.
- Same-named extension and forged JSON metadata cannot attach builtin diffs;
  denied asks do not read or write; proposed excerpt is labelled distinctly.
- Actual permission/Ink and completed transcript wiring, diagnostics coexistence,
  post-hook preservation, model/OTLP content exclusion and terminal sanitation.
- Named restored mutants remove output cap and runtime/renderer wiring; they
  must fail the behavior assertions, not just snapshots generated by the code.
- Build/typecheck, full real-Docker suite, one bounded Claude review with original
  findings and actual turns, fixes for material findings, exact-head four CI
  gates, serialized merge and all four post-main gates. Update ROADMAP/STATUS in
  this PR; optional polish goes only at the roadmap's end.

## Author verification before independent review

Build/typecheck pass. Digest-pinned required-Docker full suite: **2,976 passed,
two existing skips, 187 files, 64.26s**. Eighteen new focused core/renderer/actual
Ink controls pass; existing real checker tests now assert captured observations
alongside diagnostics. Actual 80/120-column asks and completed edits reach Ink
without a full-screen clear. Later filesystem changes leave rendered events
unchanged. OTLP spans omit captured content; model replay omits newly captured
before contents. Raw canonical logs intentionally retain the observations.

Restored mutations: removing runtime event attachment fails four actual capture
tests; removing transcript renderer wiring fails both 80/120-column Ink tests;
removing renderer line cap fails with 4,300 lines versus the limit of 80. Restored
focused run: all 18 pass. No live provider, clipboard, desktop, or browser action.

Initial fixture corrections: fresh-worktree dependencies had not yet been installed
(no tests collected); installed all workspaces. A native ESM namespace cannot be
spied on directly, so the no-read control uses forwarding mocked filesystem
exports. The initial Ink fixture used a nonexistent setAgent method; corrected to
the actual attach API. These were fixture/setup errors, not production regressions.

Independent review and final integrated PR/post-main CI remain pending.
