# R16f — truthful bounded status line

Contract before source changes. One row/PR from green main
`9aca3915e98ca8331e1c25c8a7a0eeabe27a2066`, after R15i #202 passed
CI `34072279360` and structure `34072279370` (all four checks).

The existing one-row footer prioritizes pending prompts, permission posture/live
standing-record count, actual sandbox mode, accounting, supervisor state, then
activity/model/context/session/branch. Existing viewport truncation remains the
only width owner. Sanitize bounded labels; no event/log or permission mutation.

Accounting reuses R15i SpendLedger.report and a typed read-only snapshot shared
with /cost. An actual Session object's runtime-bound segment is the only current
run identity; construction fallback segments and selected conversation IDs are
not substitutes. A maintenance child can be active while its parent remains the
selected conversation. Settled configured estimate, reservation and unknown
coverage remain distinct; unpriced consumption uses reported token snapshots,
never invented dollars. No ledger means cost unknown, with separately labelled
existing context estimate. No new token fold, price lookup, provider call or ledger
write. Reports are point-in-time estimates, not invoice/complete billing claims.

Mount-only refresh: one accounting read in flight, coalesced at most once per five
seconds, generation checks before publication and explicit stale/unavailable
state. Existing bounded ledger reads may finish after unmount but cannot publish;
shutdown joins the read. No polling in headless/ACP/MCP or unmounted controllers.
Final run completion requests a refresh; the same bound still applies. A new
active Session invalidates any preceding run even when its session ID is reused.

Live grants use inspect(), not authorization calls; count both live standing allow
and deny records, and label audit-blocked state separately. Queue count includes
actual permission and clarification queues plus outstanding supervisor question.
Posture comes from trusted effective configuration: ask means policy fallback,
not that every operation asks; custom/unknown configuration stays unknown.

Supervisor state is a typed read-only snapshot of the actual attached policy's
per-signal counters and capability-filtered rungs. Label the next candidate rung
before confidence/cooldown/progress/signature checks, not guaranteed action or a
global scalar. Custom policy stays unknown; detached/maintenance is unavailable.
No policy changes, event-inferred ladder counter or last-intervention substitute.

R15j owns committed provider selection; this row reads current typed bindings,
never command text or copied provider configuration. R16g owns maintenance and
shared idle checks; this row only exposes active-session accounting observation.

Controls: actual App narrow/wide frames; grant approve/revoke/reset/expiry;
permission/question queues and cancellation; real ledger priced/unpriced,
reservations/unknown/cache totals and /cost consistency; resumed same-ID and
maintenance-child identity; delayed stale read discarded, unavailable ledger,
unmount/headless no polling; actual policy next-rung/filtered/custom behavior.
Named mutants detected/restored; build/typecheck/full required-Docker + real
Chromium; exactly one substantive Claude review, original findings/actual turns,
material fixes only. Root merges after latest-main exact-head all-four checks.

## Author checks before the single review

The first /cost integration check caught its old fixture supplying only a recorded
session id while asserting live-run wording. Wiring the actual SessionSpendSource
like production preserves that unchanged assertion and no-new-model-call control.
Initial new startup fixtures omitted required explicit max-turn/max-output defaults;
adding the existing startup defaults fixes the fixture, not production budgets.
An initial reserved-call fixture omitted required cache rates; supplying the full
validated rates preserves the real-ledger oracle.

Named mutants detected and restored: constant-zero grant count fails actual App
approval assertions (80/120 columns); starting observers at configure time instead
of mount fails the unmounted-controller no-status/no-poll control; replacing
unpriced token/unknown display with zero dollars fails the actual unpriced-ledger
display control. Final coverage includes actual App at 32/80/120 columns.

Build and typecheck pass. Focused existing/new status, ledger and policy controls
passed 74 tests before the final startup/narrow-frame additions. Final full
required-Docker suite passes **3,037 tests plus two skips /190 files /68.06s**,
four workers; real Chromium passes one test (2.55s). Only documentation changed
during that full run; source/tests were stable. No live providers or desktop calls.
This snapshot is committed before the single bounded independent review.
