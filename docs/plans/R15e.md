# R15e — on-demand diff review

Starts from green R9c main `3b7564a`; R9c PR183 passed all four exact post-main
checks (CI34047589688 and scripted workflow34047589697). R15e is independent of
ACP and is pulled forward while R15a waits for that dependency. This does not
partially deliver R15a, R15c or R15f.

## Contract before implementation

- Extend the existing supervisor reviewer with an explicit diff mode using its
  existing AuxiliaryRun. Do not manufacture an agent trajectory or introduce a
  second review engine. Findings are advisory, not test evidence or approval.
- CLI `review` captures tracked HEAD-to-worktree changes; untracked files are
  explicitly excluded. `--base <ref>` reviews resolved commit-to-HEAD changes.
  `--pr <positive-number>` reads GitHub metadata and patch through fixed `gh`
  argv. No checkout, source fetch, tests, edits or automatic comments.
- Text diff limit **16 KiB**, **40 files**, **128 hunks**, **16 findings**.
  Findings have a validated changed-file/old-or-new hunk line, severity and bounded
  text. Response limit **32 KiB**, **4096 model events**, one model call with
  requested maximum **2048 output tokens**, existing **30 s call /90 s total**
  auxiliary deadlines. Smaller supported time/per-response limits win. Effective
  total-session `maxTokens`/`maxUsd` settings explicitly refuse before provider
  construction in standalone CLI and before any call in TUI (whose role providers
  already exist at startup); select a review profile using supported bounds.
  No total-session cap is silently discarded or claimed enforced. Missing usage
  stays unknown; requested tokens are not a hard remote billing cap.
- Git/gh subprocesses have **15 s** each within **90 s** operation lifetime,
  **64 KiB** bounded output (patch separately16KiB; metadata16KiB), joined owned
  cancellation. No shell, diff drivers, textconv, hooks, automatic network or
  model-generated argv. Unsupported/binary/oversized diff refuses before model;
  submodule output is forced to short form and changed gitlinks refuse as unsupported.
  an empty diff makes zero calls. Diagnostics do not echo raw subprocess errors.
  Local repositories with effective `filter.*` configuration refuse before diff:
  Git clean/process filters can otherwise execute during worktree conversion even
  with external diff and textconv disabled. This includes configured LFS filters.
- Capture HEAD and exact patch identity; compare again after review before
  presenting findings. Resolve refs as commits with option-safe argument handling.
  Local review requires the canonical Git top-level before patch capture; nested
  cwd refuses instead of declaring a confined read while capturing parent files.
  Explicit `--no-relative` prevents config-based coverage narrowing.
  PR metadata binds repository, base and head; recheck before an explicit comment.
  This is point-in-time observation, not atomic remote compare-and-post.
- Every gh read/write requires configured exec and net decisions; headless ask
  denies, TUI uses its current asker/registry. `--comment` requires `--pr`; no
  invalid, cancelled or incomplete-usage review is posted. Enforcing sandboxes
  refuse host git/gh, including YOLO, rather than quietly bypass containment.
- Idle TUI `/review` delegates to the same operation, reserves its command name,
  joins cancellation/shutdown, reports separate auxiliary usage and refuses while
  agent/maintenance work is active. No fabricated session or after-terminal event.
- Tests use actual local Git and local/fake provider, fixed fake gh transport;
  never live comments/model spend. Actual CLI and TUI frames, policy denial,
  malformed location/output, identity change, caps, cancellation and nonexecution
  controls precede full build/typecheck/test, named restored mutants and one
  bounded independent review. Exact-head all-four CI and root-only merge follow.

## Explicit limits

The diff is context, not proof that a claim is correct. No project source beyond
the captured patch is implicitly loaded. Local commands run trusted installed
Git/gh binaries; bounds do not sandbox hostile executable code. Concurrent writers
can change files again after the final observation. Cancelled remote model work
may keep running and its usage can remain unknown. No automatic retry or posting
is inferred from model text. Optional polish belongs at the roadmap's end.

GitHub CLI primary interfaces checked: [PR diff](https://cli.github.com/manual/gh_pr_diff),
[PR metadata](https://cli.github.com/manual/gh_pr_view), and
[comment stdin](https://cli.github.com/manual/gh_pr_comment). No `--web`, editor,
attachment, replace or delete flags are used.

## Author validation before independent review

Actual local Git/CLI and local OpenAI-adapter calls return file:line findings without
tools or mutations. A real Git-owned descendant is cancelled and joined; output
overflow refuses. Actual TUI frames display review activity/results, and busy/abort
controls prevent overlapping task starts. Configured total-session caps refuse CLI
and TUI before provider or comment calls. Fake gh transport checks exact authorized
arguments/stdin and no comments after malformed output, changed identity or unknown
usage. These are network-free fixtures, not a live GitHub comment or quality study.

Detected and restored named mutants:

- **FindingLocationGateRemoved:** invented line9 was accepted; refusal test failed.
- **GitFilterGateRemoved:** configured clean-filter work proceeded; preflight refusal
  test failed. The real fixture's filter/fsmonitor sentinel stays absent with guards.
- **ExplicitCommentGateRemoved:** an implicit PR read posted through the fake transport;
  the no-comment control failed.

Initial full combined checks on integrated R15b main74c5073: build/typecheck pass;
actual Linux container-enabled full suite **2,751 passed plus two existing skips /
160 files,49.12s**. A small subsequent parser compatibility control permits bounded
Git config subsection names such as branch.feature/review.remote; this does not
relax the filter.* refusal. Final checks and one bounded independent review follow.

A second author full run overlapped the last submodule-control edit and reported
one newly added negative test failure (2,750 passed plus two skips). It was not a
frozen validation snapshot and is not counted as a pass. The complete current
targeted suite then passed16 tests. The next full run and independent review use
an unchanged committed snapshot; no review had started during those edits.

## One independent review and bounded fixes

The frozen snapshot `3fa85e4` passed author build/typecheck and full **2,751 + two
existing skips /160 files,47.25s**. The single review independently passed typecheck,
20 focused tests and full **2,751 + two skips /160 files,47.56s**; no build claim.
Requested/reported24 turns,499.122s including an external security-hook delay, one
process, no restart. [Complete verbatim findings](R15e-review.md).

Its sole material finding was added/deleted gitlinks bypassing the modified-gitlink
gate. Actual local Git update-index/commit fixtures (no clone/network) reproduced
the pre-provider refusal failure; all three160000 mode forms now refuse.

Separately, an author-found unanswered-permission timeout reproduced through the
actual controller: the operation remained pending after its deadline. Permission
asks now receive the operation signal; exact-entry cancellation removes only that
active/queued request. Settlement is once-only before grant mutation; saved late
answers cannot grant or drain an unrelated sibling. Async policy/ask waits are
cancelled without allowing late callbacks to dispatch work. Existing callers remain
compatible. The same deadline control passed after the fix, with zero provider or
gh calls and an unrelated pending ask preserved.

An author scoped-read regression also reproduced nested cwd capturing parent files
under a falsely confined read declaration. Local review now refuses before patch
capture unless cwd is canonical Git top-level; the regression passes with zero
provider calls. No traversal or new scope framework was added. Spacing and honest
TUI preexisting-provider wording were corrected; generic diagnostic refinement is
optional at the roadmap's end. No second general review was performed.

Final ACP integration initially passed build/typecheck but exposed seven existing
scoped-grant UI failures (2,772 passed plus two skips /165 files). This was a real
author regression, not timing contention: scope editing copies prompt display state,
so cancellation must match its stable resolver identity rather than the copied object.
The fix preserves exact-request/once-only settlement. A new scope-copy cancellation
control fails with the old comparison restored, passes with the fix, and keeps an
unrelated sibling pending while a saved late resolver cannot grant. The mutation was
removed. Combined review/scoped-grant/child/grant-inspection/ACP tests pass **51/5 files**.
Final unchanged-snapshot full validation follows; no new independent review.

ACP PR184 is done: post-main `0493b09` CI `34049720509` and structure `34049720468`
all green; [receipt](https://github.com/agentkitai/agentrig/pull/184#issuecomment-5561091432).
R15b PR185 is also done with all four post-main checks green. Both integrations retain
their runtime behavior; review never launches configured post-edit checkers.

Final unchanged production snapshot `725f3b5` on ACP main `0493b09`: build and
typecheck pass; full actual-Linux-container suite **2,780 passed plus two existing
skips /165 files, 48.81s**, four workers. This includes the scoped-copy cancellation
control after restoring its detected mutation. Hosted exact-head gates remain pending.
