# R15f — bounded CI / bot mode

Contract checkpoint, before implementation. Base: green R15e main
`eead6873131b621639c1b6162e4dfc555253049f` (PR #186), with exact post-main
CI `34051436203` and scripted structure `34051436058` all green.
R15f reuses R12c permissions/reporting and R15e host-process/comment seams; it is
independent of the concurrent R15a questions, R15d remote MCP and R8b server rows.
No partial completion of those rows, new milestone hierarchy or hosted component.

## Input and runtime

- `agentrig run --ci` requires exactly one explicit `--task-file <path>` or
  `--event-file <path> --event-field <selector>`. The only selectors are
  `issue.body`, `comment.body`, and `pull_request.body`; no expression evaluation.
  Positional tasks, resume and CI-only flags outside CI mode refuse rather than
  create ambiguous authority. Existing ordinary run behavior stays unchanged.
  Effective JSON/verbose output also refuses before provider work rather than
  silently bypassing or ignoring the bounded report format (root-approved clarification).
- Files must be stable regular non-symlink UTF-8 inputs. Task text is nonblank and
  at most 16 KiB; JSON event input is at most 256 KiB and depth 16. Bounded reads
  and strict selected-value validation precede any provider work. Unselected
  event fields are opaque, never config, environment, repository or argv input.
  No implicit `GITHUB_EVENT_PATH` read, checkout, fetch or payload execution.
- The actual runtime task is empty, with the selected text passed through the
  existing `advisoryContext` seam. A fixed host system instruction explains its
  CI purpose. No nonblank synthetic user prompt or fresh-user reset is minted.
  Payload text cannot grant permission or clear the external-input restriction.
- Reuse `runCommand` / `buildAgent`, with a small trusted observer/ask/advisory
  input seam, not a second agent loop. CI is always headless. Effective YOLO or
  dangerously-skip-permissions refuses before execution; other explicit policy,
  trust, sandbox and provider settings keep their existing meaning.
- Actual unresolved permission asks deny and abort through the live runtime,
  stopping queued effects and forcing nonzero exit even if a later model result
  says done. Explicit denials never become consent. No interactive fallback.
- Fixed CI ceilings are 20 turns, five minutes and 50,000 main-model tokens;
  smaller configured values win and clamping is reported. Existing per-response
  and priced `maxUsd` validation remain. These are the existing runtime's limits,
  not aggregate auxiliary/child or hard remote billing caps. Cooperative abort
  and existing cleanup limitations stay explicit. No provider or live eval spend
  is used to implement or test this row.

## Reporting and optional comment

- Require explicit `--report <path>` and/or the complete
  `--pr <number> --repo <owner/repo> --comment` combination. Payload fields never
  choose output paths or PR targets. These are literal operator-selected outputs.
- File output is create-only under a canonical existing parent: no directory
  creation, symlink target or silent overwrite. The explicit report flag grants
  this bounded host output operation, not model file-writing authority. A report
  is reserved before provider work so occupied/unsafe destinations refuse early.
- Markdown is at most 64 KiB. Fixed fields record outcome, session identity,
  effective limits, permission refusal counts, main usage and honest auxiliary
  coverage. Retain at most 16 KiB of final assistant text, with explicit omission;
  no raw tool outputs, task bodies, provider errors, environment or credential
  bundles. Clone and reuse existing export redaction for report-only content;
  logs are never rewritten. Render model text inertly and escape control content.
  Heuristic redaction can still miss arbitrary secrets; no secret-free guarantee.
- A local report remains available on refusal, runtime failure or cooperative
  cancellation, with partial/unknown status explicit. Output failure is nonzero.
  Comment-only failure still emits a bounded safe diagnostic; no implicit file.
- Reuse R15e fixed-argv owned GitHub process, authorization and identity checks.
  Every `gh` read/post needs configured exec and net permission, including metadata
  reads; asks deny. Host `gh` refuses under an enforcing sandbox, even if a caller
  tried YOLO. Pin repository, PR number and base/head before execution and recheck
  before posting. One explicit comment, no edits/retries or model text as argv.
  No comment after cancellation, invalid outcome or incomplete accounting.
  Remote check-and-post is point-in-time, not transactional or attested identity.

## Acceptance and delivery

Real CLI with local fake adapter: golden successful report; ask-class tool exits
nonzero, dispatch sentinel absent, subsequent queued effect absent, report written.
Actual advisory provenance/continued state cannot gain fresh user authorization.
Configured smaller budgets and YOLO refusal, malformed/oversized/deep input,
selector ambiguity, occupied/symlink report destination, secret/control canaries,
cancellation and partial output all have network-free positive/negative controls.
Fake `gh` covers permission denial, pinned identity changes, explicit-only comment
and cancellation/accounting refusal; no live posts. Preserve R15e tests if its
reusable helper is extracted. Integrate R15a question events if it lands first,
without depending on or implementing that row here.

One documented GitHub Actions example (not installed): trusted pinned checkout,
read-only default token permissions, literal input path/selector, explicit narrow
profile, bounded job, always-upload report. No untrusted event-derived checkout or
shell interpolation, no workflow service or default unattended deployment.

Full build/typecheck and actual-container full tests, detected/restored named
negative controls, exactly one bounded independent Claude review and material fixes.
Keep optional polish at ROADMAP END. Own PR updated to latest main, all four
exact-head checks green; root merges and verifies all four post-main checks.

## Initial implementation evidence (before independent review)

The existing runtime is reused through optional trusted `RunCommandDependencies`;
ordinary two-argument calls retain their behavior. R15e and R15f share the bounded
`gitHubRequest` authorization/process seam. Initial build/typecheck and 66 focused
CI/review/program/accounting tests passed. Actual subprocess fixtures cover normal
and advisory read completion, queued-write ask refusal, payload cannot mint fresh
exec approval even with an exec allow rule, report redaction/inert rendering,
configured YOLO, file bounds/links, and a real pending-provider deadline retaining
partial text. The first trust assertion looked in JSONL rather than the snapshot;
it now inspects the actual persisted external/advisory block, not a fabricated label.

Named negative controls, restored after detection:

- `CiFreshUserReset`: replacing the empty task with synthetic user text changed
  the explicit-exec negative from one stopped request to 20 requests; the actual
  CLI control failed. Empty task restored.
- `CiIncompleteCommentAllowed`: removing the complete-accounting gate let the
  fake-GitHub partial-usage case succeed; its nonzero/no-post control failed.
- `CiAskAutoAllowed`: replacing actual deny+abort with allow made the real queued
  write refusal control fail. The fail-closed callback was restored.

At this initial checkpoint no independent review had run. No live provider, live PR comment or hosted
example was executed. Actions documentation uses primary GitHub secure-use/event
guidance and tag-resolved upstream action commit pins, linked in [CI mode](../CI-MODE.md).

Initial unchanged full suite on base `eead687`: build/typecheck pass; **2,790 tests
passed plus two existing skips /166 files, 54.89s**, four workers with actual Linux
containers. Subsequent pre-review report controls bound redaction/render expansion
and distinguish publication refusal from successful runtime completion. Report
reservation may remain empty after an uncatchable crash; ordinary failures write
an explicit failure/unknown report. Final integrated validation still follows.

Integrated green R8b main `6322139` (post-main CI `34053014401` and structure
`34053014398`): frozen install, build and typecheck pass. The first full command
mistyped the checker image digest with an extra character; its two real-container
controls failed before useful execution (2,803 passed plus two skips). That is an
author invocation error, not green container evidence or a product/test defect.
With the exact correct pinned image and unchanged source snapshot `dba032a`, full
suite passes **2,805 plus two existing skips /169 files, 55.07s**, four workers,
including real Linux containers. No assertion, timeout or product limit changed.

The documented profile is executed through the actual CLI: it caught an invalid
plural `extensions` key, now corrected to `extension`; smaller configured ceilings
reach the report. JSON/verbose output refuses explicitly; those flags remain
invalid project-config fields. Publication failure records a distinct outcome from
runtime completion, and expanded sanitized output has a separate bounded render
cap with explicit omission. Independent review starts only after this frozen gate.

## Independent review and readiness

The single read-only Claude review of frozen `b7df8a5` returned **APPROVE**, with
no material findings. Its full final response is [preserved verbatim](R15f-review.md).
Requested maximum 24 turns; the CLI reported 35 turns and 387.218 seconds, exceeding
the requested cap. There was no restart or second review. Independently: focused
66 tests /4 files passed, then full **2,805 plus two skips /169 files, 52.20s**
with the exact real-container images. The reviewer did not build or typecheck;
those remain the author's separate green checks. Its listed focused command had
one nonexistent path; the actual four existing files and count are stated honestly.

Optional capture/render wording was clarified without a production change; other
cosmetic notes are at ROADMAP END. Upstream Actions pins were checked by the author
before review, not by the network-disabled reviewer. No live provider calls or PR
comments were made; local fake HTTP adapters were exercised by subprocess tests.
Final exact-head PR checks and root merge/post-main gates remain pending.

Root independently checked all four upstream Actions refs after review. The
author's original pnpm/action-setup v4 ref `f40ffcd9367d9f12939873eb1018b921a783ffaa`
was an annotated tag object, not a full commit. The uninstalled example now uses
its peeled commit `b906affcce14559ad1aafd4ab0e942779e9f58b1`; the other three refs
were commits. This is a root-found documentation correction, not a Claude finding
or a second review. The original author's claim of verified commit pins was too broad.

The first PR head `4fad6e7` passed Linux/macOS/structure but Windows CI
`34054298107`, job `101543231905`, failed only the new documented-profile
fixture: LF-only Markdown fence extraction returned undefined on CRLF checkout
before provider work (7ms). The other ten CI tests passed, including actual
permission refusal, cancellation and report controls. No unchanged-head rerun.
The fixture now explicitly executes both LF and CRLF forms of the real documented
profile. Before the delimiter fix, LF passed and CRLF failed at the same assertion;
after accepting `\r?\n`, all 12 CI tests pass (10.30s). Semantic assertions,
actual CLI execution, production limits and test timeouts are unchanged.
Post-repair build/typecheck pass; unchanged production full suite passes **2,806
plus two skips /169 files, 53.94s**, four workers and actual Linux containers.
Main integration is held for the separate R15a post-main fixture repair; this
successful local/PR snapshot does not claim the blocked main gate is green.

After repaired main `3174475` passed all four post-main checks, integrated R15a
and repair #191, preserving ordinary `--answer-policy` and `onQuestion` wiring.
Actual CI controls prove default unavailable clarification ends nonzero before a
same-batch explicitly allowed write; explicit first-option automation then attempts
an explicitly allowed exec but still cannot gain fresh consent or dispatch it.
Reports add only protected question/answered/unanswered counts, not question/answer
prose. Combined 74 focused CI/question/ACP/review/program tests pass. Frozen install,
build and typecheck pass; full suite **2,837 plus two skips /172 files, 53.55s**,
four workers with actual Linux containers. No second general review.

The previous `c7a8b68` push had no fresh Actions runs: its PR merge conflicted with
then-red main. It was not a running or passing CI head. This resolved integration
now supplies the next exact-head CI candidate, still pending its own four checks.

Integrated R15d main `8da49d4` only after CI `34057526815` and structure
`34057526836` passed all four post-main gates. Retained its startup signal and
interactive callback behavior; CI's trusted deny+abort callback now covers
`onStartupAsk` as well as runtime `onAsk`, with bounded quiet diagnostics.
The actual remote-MCP startup control observes no HTTP or provider requests.
`CiStartupAskOmitted` (removing only that callback mapping) made the same test
observe one provider request and fail; mapping restored. Combined 70 focused
CI/remote-runtime/question/review/program tests pass. No second broad review.
After restoring the mutation, frozen install/build/typecheck pass; full suite
passes **2,869 plus two existing skips /176 files, 62.69s**, four workers and
actual Linux containers. Exact-head CI remains required on this integrated source.
