# R17b — Defaults pass

Builder: **agentrig**, continuation session `b9109d9f`; conductor `8fbde1a3`.
Continue the existing `feat/r17b-defaults` / PR #234. The first baseline-only
commit `cf3ed28` and subsequent baseline evidence commit are preserved, not rebuilt.
R17a artifacts stay in `docs/plans/R17-baseline.md` and `.agentrig/r17/`.

## Scope and implementation

CLI config adds the built-in lowest-precedence recommended profile. It enables
supervisor heuristics, diagnostics presets using existing parsers, bell notifications,
checkpoints, session-end ingest and tool summaries. TUI Markdown, prompt history and
raw thinking preservation already existed/defaulted on; their implementation remains
unchanged. Explicit config false/empty overrides work. SDK defaults do not change.
The authoritative migration table in `docs/DEFAULTS.md` lists all seven removed flags:
`--supervise`, `--no-supervise`, `--checkpoints`, `--ingest-on-end`,
`--no-ingest-on-end`, `--notifications <mode>`, `--verbose`.
No security flags or defaults move, and no unrelated help options are hidden.

The only approved deviation is arbiter `1bb8ec86`'s sandbox exception. The roadmap
changes only the exact approved R17b phrase. Implicit host hooks are omitted under
enforcing sandbox with startup and mounted-UI notices; explicit opt-ins reach the
unchanged core fail-closed error. Acceptance and the band renunciation are unchanged.

## Observable checks

- Initial fail-first `recommended-defaults.test.ts`: 6 failures on the old behavior.
- A temporary recommended-layer-disabled mutation, followed by `pnpm build` and
  `pnpm exec vitest run packages/cli/test/recommended-runtime.test.ts packages/cli/test/recommended-defaults.test.ts`,
  fails 5 tests including real-CLI checkpoint creation. Layer restored immediately.
- Real CLI fresh repository, no config: local-provider runtime test observes a
  checkpoint, diagnostics line with unattended exec honestly denied, ingest completion,
  and preserved raw Markdown. Two real-builder guard tests prove explicit checkpoint
  and ingest opt-ins still hit the core enforcing-sandbox startup error.
- `python3 .agentrig/r17/recommended-smoke.py`: real TUI PTY acceptance, local fixture
  provider; final frame renders the Markdown heading and bold text, shows diagnostics
  after edit and completed ingest, and raw log includes checkpoint.created. Project
  trust and checker exec prompts were declined; only fixture write approval was given.
  Recorded session `2f761678`, evidence `docs/plans/R17b-smoke.json`. Temporary raw
  fixture logs are cleaned; this is recorded fixture output, not durable live telemetry.
- Existing single-purpose integration fixtures explicitly opt out of auxiliary ingest,
  or recognize its separate request, rather than silently treating it as a main turn.
- Root help has 2 options (baseline also 2), strictly below 40.
- Final local trio: build/test/typecheck all exit 0; 211 test files, 3337 passed /
  4 skipped. Protocol notice redaction regression also failed first, then passed.
  Exact-head CI is reported in the PR after push.

## Friction and boundaries

Feel #232/#233 were already filed. #233 is covered by the approved exception.
Feel #235 was filed with label `feel` by this continuation, before any next child,
with exact command, event message and canonical parent-session reference. It captures
session-end memory writes invalidating a terminal checkpoint ownership seal. Raw
checkpoint creation works; undo can remain unavailable. No relaxed ownership check,
expanded exclusion, new sandbox-safe hook runner or other capability was introduced.

Third R17b child (builder, arbiter, continuation), prior human pauses **1**. Observable
token accounting is recorded as a canonical-events snapshot in STATUS/PR; ongoing
continuation totals are explicitly partial. Independent external review belongs to
the conductor after this child stops. No external reviews or merge in this child.
