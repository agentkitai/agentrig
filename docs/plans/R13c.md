# R13c — external-input permission expansion guard

This is one coarse enforced guard, not a permission framework or a solution to prompt injection.
Three first-actual-dispatch categories are tracked per run: exec, network, and write outside or
unknown relative to cwd. A grant, proposed tool.call, denied call or sandbox-preparation refusal
does not open a category. Actual invocation opens it even if execution subsequently fails.
Resume starts empty and restricted; no log receipt restores this live state. Actual child creation
inherits only the runtime parent's current restriction via internal object-bound WeakMap metadata;
copied/serialized options or absent state remain restricted. Generated child briefs cannot clear
inherited restriction. A clean parent's ordinary headless child remains usable. No argv/URL/path-history claim.

New input is runtime-delivered non-model content since the preceding request boundary, not old
task/history text. External, generated and unlabeled ancestry is conservative; recognized
tool-output alone is neutral, as are user/project labels, and none clears an existing restriction.
Generic shell/custom output is not automatically proven external: this is limited source tracking,
not injection prevention. Ordinary edit→test workflows remain usable headless. Restriction is
sticky through no-new-input continuations and compaction. Only an actual fresh nonempty user task
or delivered user steer clears it; roles, stored/returned context labels, prose and hook delegation
cannot manufacture fresh user input. A request containing actual fresh user input is the explicit
mixed-input exception; this does not claim semantic intent attribution.

Under restriction, first-category expansion requires fresh separate consent despite blanket/yolo,
standing or scoped allow grants. Applicable base and live standing/scoped denials still win on the
original request; fresh consent cannot override them, including a matching live deny behind an
earlier overlapping allow. Ordinary first-match grant/base-policy ordering is unchanged outside
this additional restriction. No interactive callback means deny;
only explicit fresh allow succeeds. SDK onAsk is a trusted human-approval seam, not proof that an
arbitrary host callback really asked a person. The reserved external-input-expansion origin cannot
be remembered, and original child provenance is preserved separately. Distinct permission.expansion
events audit the decision. Approval alone never marks actual dispatch.

Write classification reuses the existing bounded canonical write-target resolver, including new
files, missing parents and dangling symlinks. Missing declarations, errors, over-budget or
unverifiable containment require approval. This trusts registered permission/path declarations
and observes the filesystem; it does not attest tool effects, external-writer races or hardlink
aliases, and it does not replace sandbox enforcement.

A bounded heuristic injection detector scans explicitly external canonical content and summaries
for instruction-shaped phrases/tool syntax. It emits an advisory injection signal, with false
positives/negatives explicitly possible. The standard ladder permits guidance only for this signal,
even with every escalation capability enabled. It never creates authority or supplies the enforced guard.

Acceptance: actual same-action user-versus-external and summary-laundering pairs; sticky state,
fresh-user reset, per-category/actual-dispatch/resume cases; canonical/symlink/unknown write paths;
fresh approval bypass/denial/cancellation, child attribution and UI no-standing/no-yolo controls;
real supervisor attachment, schemas/rendering, named restored mutation controls, full checks and
one bounded independent Claude review. Each exact updated-main head needs all-platform CI; root
merges and post-merge CI gates dependent work. Optional polish belongs at ROADMAP's end.

Pre-review validation checkpoint: build/typecheck and full four-worker suite passed 2,263 tests plus two
skips (119 files). Initial integration failures were retained as diagnostics: cancellation during
the new async classification now returns an aborted tool result instead of a fatal session error;
the review exposed consent overrides in checkpoint/grandchild fixtures as a usability regression.
Those overrides were removed after neutral tool-output/live child inheritance fixes; their original
effects/lifecycle assertions pass unchanged. The old standing-deny assertion remains intact. Direct already-aborted
subagent execution retains its original regression separately from new no-dispatch protection.

Negative controls: removing fresh-consent forcing failed all four same-action pairs. Ignoring
external input initially escaped the combined summary+hook test because the hook independently
restricted the request; removing that confounder made the built-in summary test fail as intended.
Both mutants were restored; the corrected summary test and 69 focused cases passed. A third mutation
removing deny-only selection failed the overlapping allow-before-deny control and was restored.

One bounded independent Claude review used session `0225d5ce-58ab-4bbf-be2a-3709deccfeab`, reaching
the 16-turn cap (17 reported turns including terminal handling, 343 seconds). Its written verdict
was REQUEST_CHANGES; the cap prevented a final formatted reply. Original findings and resolutions
are preserved in [review receipt](R13c-review.md), without a second general review. Its HIGH/LOW
usability findings changed the contract as above; its MEDIUM heuristic-escalation regression failed
before the guidance-only fix, then passed with real repeated runtime signals/full capabilities.
Final updated-main validation and exact-head all-platform CI remain required.

Final pre-PR integration: current main `7f01c4c` (R5a and fourth-pass roadmap) merged without
production conflicts, retaining extension receipts/identities and no child extension inheritance.
Build/typecheck and the complete four-worker suite pass: 2,292 tests plus two skips, 122 files,
30 seconds. A further child-restriction-laundering mutant failed the external-parent fixture and
was restored. Windows CI explicitly runs the new core, actual CLI and supervisor tests.
