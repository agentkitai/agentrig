# R16g — owned manual TUI maintenance

Started from all-four-green main `9aca3915e98ca8331e1c25c8a7a0eeabe27a2066`
(R15i #202; post-main CI34072279360 / structure34072279370).
One roadmap row, one implementation PR, one bounded independent Claude review.

## Contract

- `/clear` is the existing `/new` operation with identical busy checks and grant
  reset; same trusted configuration, no model call.
- `/doctor` runs the existing plain diagnostic with `probe: false`, bounded
  terminal-safe output, no model or credential refresh. It reports configuration,
  not live provider availability. Existing filesystem probes are cooperative and
  cannot be forcibly cancelled; no hidden background repeat/probe is introduced.
- `/diff [checkpoint [turn]]` captures tracked worktree changes against HEAD or a
  validated checkpoint belonging to the selected session. Reuse protected Git
  subprocess execution, filter refusal, canonical root checks, permission checks,
  cancellation, identity recheck and output limits from local diff review. No
  reviewer, GitHub calls, external diff/textconv/filter execution or provider.
  Untracked/binary coverage is explicit; this is not an undo or ownership claim.
- `/compact` is an explicit owned core maintenance operation, not an empty normal
  user turn. It accepts a finished persisted conversation and uses existing
  compaction/provenance, store locks, session forks and lifecycle primitives.
  No ordinary model loop, tool dispatch, user-prompt/session-end hooks, supervisor,
  final-output repair, memory ingest or dream is launched.
- Compaction operates on an explicit child fork. The original selected session,
  raw log and snapshot remain unchanged until a completed child has a validated
  canonical compaction receipt, atomic snapshot and terminal event. The controller
  adopts only after successful verification and grant reset. Failure/veto/no-op
  keeps the original selected. Partial children remain unadopted diagnostic
  artifacts, never silently deleted or advertised as completed maintenance.
  This does not claim atomicity across a JSONL append and snapshot rename.
- Preserve the verified parent task, usage, roles/provenance and retained opaque
  reasoning/tool pairs. Summaries remain advisory. No placeholder user text is
  allowed to clear external-input restrictions or mint consent.
- At most one existing compactor invocation, deadline at most 60 seconds (smaller
  configured wall-time limit wins), bounded summary output, existing R15i meter
  exactly once. Unsupported legacy total-token/USD caps refuse before spending;
  they are not silently ignored or reset. No live provider calls in validation.
- The printed manifest delta describes transcript-only byte/token estimates;
  system/tool request context is excluded. Do not forge a request hash for a
  request never sent. The next ordinary request still emits its actual manifest.
- One synchronous idle/busy guard covers active runs, queued/active permissions,
  questions, escalation, undo, dream, review, selection preparation and maintenance.
  Abort/shutdown join owned work. Read-only status invalidation follows actual
  maintenance segment identity, not the still-selected parent conversation.

## Acceptance controls

Actual persisted compaction followed by fresh-store resume and fork; parent
byte/snapshot identity on success and every failure; old external restrictions and
opaque retained pairs; native output configuration without a repair; actual capped
adapter admission/no-dispatch refusal and unknown usage; veto/no-op/abort/deadline;
held lock, concurrent command, append/snapshot/terminal persistence failures; no
normal hooks/tools/model turns. Actual Ink commands, `/clear` parity, model-free
doctor, real Git tracked changes/checkpoint validation/filters/permission denial,
terminal sanitation and bounded output. Named restored mutants must discriminate
the actual wiring and publication guards. Full build/typecheck, required Docker
suite, actual Chromium and all four exact-head/post-main CI gates.

## Validation and review

Implementation in progress; no independent review verdict yet. Actual core,
controller, Ink/startTui, local diagnostics, Git/checkpointer and metered fixtures
exercise the production seams with no live provider. Three restored mutants fail:
removing post-terminal publication verification adopts a failed child; removing
startup command wiring loses doctor/diff; removing checkpoint-ref verification
accepts a moved ref. Each restored before passing the same controls.

Read-only overlap inspection identified two concrete reentrant ownership cases:
replacement Agent during awaited compaction and synchronous shutdown inside
Agent.run before its Session is returned. Both author controls failed before the
captured-agent identity / post-return cancellation guards. Ownership promises are
reserved before invoking callbacks. Native-output maintenance starts no JSON repair,
retained signed reasoning is byte-identical, custom compaction cannot mint user
authority, and empty-task resume still refuses an effect without new consent.
Unmetered coverage remains unknown; missing usage blocks the next capped dispatch.

Green-main integration `b3599ca` incorporates delivered R16d (#200) with only
documentation conflicts; source merged automatically. Build/typecheck and required
Docker full pass **3,087 tests plus two existing skips /194 files, 70.21s**;
real Chromium smoke passes (one, 2.39s). A final author stale-snapshot control then
failed before canonical-parent message comparison and passes afterward, refusing
before a fork/provider call. All 35 dedicated controls pass; final post-fix full
validation and the single independent review follow.

Post-stale-snapshot build/typecheck and full required-Docker pass **3,088 plus two
existing skips /194 files, 67.99s**, plus real Chromium (one, 2.35s). The final
author overlap control reproduced `/fork` preparation admitting `/compact` and
`/clear`; the same reserved startup ownership now blocks overlap and joins the
fork without adopting its result after abort. Dedicated controls pass with this
fix. This source is frozen for one Claude review and combined full validation;
no live provider or clipboard operation is used.
