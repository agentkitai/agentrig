# R10d — explicit empirical provider conformance probes

`agentrig doctor --probe` deliberately performs potentially billable network requests to the
resolved main provider entry, after ordinary configuration/trust/credential diagnostics. It
does not construct other role providers, tools, MCP servers or extensions. Plain doctor never
constructs a provider, refreshes auth, performs a model request or writes probe observations.

Core probes at most five outer `stream` invocations, requesting at most 256 output tokens each,
with 256-event/8-tool/16,384-character per-stream collection limits and a 30-second total local
deadline. Cancellation is propagated and a stuck custom iterator cannot stall the runner.
Transient HTTP/stream retries are disabled for the CLI probe; ChatGPT retains one forced 401
refresh/re-request and auth may refresh/persist rotated tokens. Requested output limits do not
constitute a remote billing cap (ChatGPT ignores the parameter); cancellation cannot prove
remote work stopped. Reported usage is aggregated separately from its completeness. Missing,
synthesized, interrupted or retried usage is not presented as known zero cost. No tools execute.

Samples check an exact inert tool request/result acknowledgement, two distinct valid calls in
one response, prompted JSON schema conformance, and repeated stable-request cache reporting.
Prompted JSON matching is empirical; native/wire structured strictness stays explicitly unknown
because ModelRequest exposes no native strict-format control. Completed mismatches are
not-observed; incomplete/error samples are unknown. Positive reported cache counts demonstrate
a cache observation; absent/zero counts cannot disprove caching. These are samples, not universal
guarantees, model attestation or permission authority.

## Cache and consumption

The normal CLI provider construction path reads fresh matching observations and adjusts actual
tools/parallelTools/caching capability booleans for observed/not-observed dimensions. Unknown
dimensions retain their existing configured values, explicitly labelled unverified-configured
per dimension. Context window, pricing, permission and sandbox behavior are unchanged. Direct
SDK constructors retain defaults unless the caller explicitly applies observations.

Mutable local advisory cache: versioned validated JSON, at most 64 entries/128 KiB, 24-hour TTL,
future/stale/corrupt/mismatched entries ignored. The key hashes provider/model/endpoint/tuning
and actual credential material. No raw endpoints, secrets, response text or provider errors are
persisted in reports. ChatGPT matches file-first valid-token/env fallback, without auth refresh
on reads; unavailable or changed-during-probe identity prevents caching. Token rotation can
invalidate a prior entry. The cache is not a server attestation or an isolation boundary.

Writes use exclusive random temporary files and atomic replacement, retaining bounded recent
entries. Concurrent writers may lose another observation (last replacement wins), conservatively
falling back to unverified configuration; no permission record is created or restored. Local
owner edits/global host customization remain trusted. Default provider construction reads only;
only explicit probe writes. Errors are generic to avoid printing credential-bearing URLs/body.

## Verification plan

Fake core streams and actual local HTTP OpenAI SSE exercise the real doctor→adapter→cache→builder
path without live model spend. Tests discriminate normal doctor, exact roundtrip/parallel/schema,
missing/synthesized usage, zero cache counts, deadline/abort, limits, retry-disabled HTTP failures,
secret-safe errors, malformed/oversized/expired caches, exact configuration/credential changes,
and consumed capability flags. Named mutations, one bounded independent review, full checks and
exact-head three-platform CI precede delivery.

Base includes R11b #168/main `4f61b65` (post-CI 34032134303 all green) and integrated R14c #169/main
`4d3eb91` (post-CI 34032607190 all green). R14c config-free sessions-show evidence remains separate.

## Verification receipt

Final combined build/typecheck and full 2,491 tests plus two skips across 142 files pass
(four workers, 33 seconds). Capability-application removal failed the actual local SSE→doctor→
cache→builder control; unconditional-probe mutation failed the ordinary doctor factory guard.
Both restored. One independent read-only Claude review approved, no material findings:
`39b8e8f4-4f8c-4366-a39f-d99ac12ff798`, 178 seconds, 23 reported/max24 turns. It independently
ran 17 focused tests and typecheck. No agents/tool denials/live probes. Verbatim final result:

> **Verdict: APPROVE.** No material findings. The R10d diff matches the approved contract in docs/plans/R10d.md on every point checked. Typecheck is green and both new test files pass locally (17 tests, fake core streams and local HTTP SSE only).
>
> **Verified against the contract**
>
> - **Normal doctor stays offline.** The probe block in `packages/cli/src/doctor.ts:541` runs only when the parsed `--probe` flag is true. Plain doctor never calls a provider builder, never touches auth, and never writes the cache. The first CLI test asserts the factory is uncalled and the cache file is absent.
> - **Explicit probe is main-only and bounded.** Doctor builds one provider via `buildRoleProvider(..., "main")`. The core runner caps five outer streams, 256 requested tokens, 256 events, 8 tool calls, 16,384 characters per stream, and a 30-second deadline. The deadline aborts through `AbortSignal.any`, and a stuck iterator's `return()` is not awaited, so it cannot stall.
> - **Retry tuning is probe-only.** The `maxRetries: 0` spread in `packages/cli/src/provider.ts:107` applies only under the probe hook. All three adapters accept the `retry` option. The local 500 test confirms ordinary construction still retries once while the probe sends exactly one transport per sample.
> - **Usage is aggregated separately from completeness.** Synthesized usage is excluded and marks the run incomplete. Retries, missing usage, limit violations, and aborted samples all force `usageComplete=false`, and zero streams does too.
> - **Exactness of samples.** Roundtrip requires a nonempty id, exact name, strict `{value:"probe"}` input, and the exact echoed text with no further calls. Parallel requires two distinct ids with both exact calls. Prompted JSON is strict-parsed. Native strictness is a zod literal `"unknown"` and printed as such.
> - **Cached observations drive flags.** The builder applies observed and not-observed dimensions to the real capability booleans and leaves unknown dimensions on their configured value, labelled `unverified-configured` per dimension. Context window and pricing are untouched. The CLI test shows a not-observed report flipping tools and parallelTools to false through the ordinary builder without any further request.
> - **Matching and cache bounds.** The fingerprint hashes provider, model, resolved endpoint, contextWindow, reasoningEffort, and credential material. The ChatGPT branch mirrors the store's file-first, corrupt-file-to-env-seed precedence and returns undefined when the bounded read cannot decide. Read and write cap at 128 KiB, 64 entries, 24-hour TTL, future timestamps ignored, strict versioned schema. Writes use a random `wx` temp file plus rename. Fingerprint is recomputed after the probe so token rotation during the sample skips caching.
> - **No secrets.** Only the sha256 digest is stored. Doctor catches every probe exception with a generic line. Tests assert the API key, base URL, and response text never appear in output or on disk.
>
> **Optional polish, non-blocking**
>
> - `packages/core/src/provider-conformance.ts:102` sets the top-level `source` to `"observed"` whenever a report exists, even if every dimension is unknown. The per-dimension `sources` remain correct, but a consumer reading only the summary field could over-trust it.
> - `docs/STATUS.md:104` has a typo, "across142 files".

Clarification of the review's shorthand: ordinary doctor's existing read-only credential/token
inspection remains; it never constructs/refreshes provider auth. Only digests plus bounded numeric/
enum observations are stored, not raw credentials. The top-level source note is END polish;
per-dimension source is authoritative. The documentation typo is corrected here.
