// Full unmodified reviewer outputs from PR455 halt comment 5753041906.
export const preservedReviews = [
  "Reviewed head a45dc1f93db4a581371d0b94f0e7e518347fd890\n\nVERDICT: PASS\n\nFocused material-delta review, repair round 2/3. OLD `aaa64642228fa35ccd3d176dd40de3e7fd87ca49` → NEW `a45dc1f93db4a581371d0b94f0e7e518347fd890` (single commit `a45dc1f`; OLD and `origin/main` `6e0ec1f5181d34209e3fc557acc6c29d10984594` are both ancestors of NEW, verified with `git merge-base --is-ancestor`; no integration commit). Scope reviewed: the OLD..NEW delta and its direct interactions. Unchanged code, closed blockers and rebutted scope preferences were not re-audited.\n\nResult: the assigned blocker is closed. One LOW nonblocking finding and three nonblocking observations; nothing blocking.\n\n## Inputs verified before reviewing\n\nConductor receipts `/private/tmp/ship455-r2/checks.md`: head `a45dc1f93db4a581371d0b94f0e7e518347fd890` matching my review target, worktree `/Users/amit/ship455-r2-proof`, bootstrap exit 0 in both trees, preflight 21:50:56Z–21:50:57Z, build 21:50:57Z–21:51:05Z, test 21:51:05Z–21:52:09Z, typecheck 21:52:09Z–21:52:14Z, all 2026-09-20 UTC and all exit 0. I ran no declared check, no bootstrap and no preflight; my tree was conductor-prepared with dependencies and build outputs, clean at NEW. Live PR head read back as `a45dc1f93db4a581371d0b94f0e7e518347fd890`, so this review is of the current head. Live PR body, comment 5752832259 and issue 453 all read live and matched the dispatch brief.\n\nDelta: three files — `scripts/review-finding-index.mjs` (net −13 lines), `packages/cli/test/post-review-comment.test.ts`, `docs/STATUS.md`. No product code, no events, no skills, no config, no `docs/ROADMAP.md`, no `package.json` and no lockfile change. `marked@18.0.11` was already a declared dependency of `packages/cli/package.json` at OLD, so nothing was added to satisfy the operator's direction. No deviation record is required.\n\n## Assigned blocker disposition\n\n**CLOSED.** The assigned heading was:\n\n> ### MEDIUM: A blank line inside a list item turns a lazy list paragraph back into a permitted citation, re-opening the F3 echo leak\n\n`scripts/review-finding-index.mjs:40-62` replaces the hand-rolled line scanner with a walk over `lexer(body)` tokens. Citation permission is now granted only by skipping a `code` or `blockquote` block token while a finding section is open; `list` tokens are traversed into their items, so a list item's paragraphs are re-entered as prose and stay echo-checked. That is the structural property the acceptance asked for, and it is implemented through the tokenizer rather than by patching indentation arithmetic.\n\nDifferential probe, OLD helper versus NEW helper on `assertReviewerVerdict`, using the first pinned instruction sentence as the echo. Every row of the reproducer named in the dispatch flips in the refusing direction:\n\n| Body (all after an indexed finding heading) | OLD | NEW | Rendered |\n|---|---|---|---|\n| `- List paragraph.`, blank, four-space echo | ACCEPT | **REFUSE** | visible prose |\n| `1. Ordered paragraph.`, blank, four-space echo | ACCEPT | **REFUSE** | visible prose |\n| `- List paragraph.`, blank, tab echo | ACCEPT | **REFUSE** | visible prose |\n| `1) Ordered paragraph.`, blank, tab echo | ACCEPT | **REFUSE** | visible prose |\n| `* ` and `+ ` bullets, blank, four/five-space echo | ACCEPT | **REFUSE** | visible prose |\n| `10. Ordered paragraph.`, blank, four- and five-space echo | ACCEPT | **REFUSE** | visible prose |\n| nested `- Outer` / four-space `- Inner`, blank, eight-space echo | ACCEPT | **REFUSE** | visible prose |\n| tight bullet-then-indent and tight prose-then-indent | REFUSE | REFUSE | visible prose |\n\nPositive controls all still post: fenced, tilde-fenced, indented-code and blockquote citations inside a finding; a genuine nested code block inside a list item at six and eight spaces; a top-level indented code block after a list closes; a fence nested inside a list item; a four-space-indented fence. Negative controls all still refuse: every permitted form placed outside a finding section, plain prose, bullet prose, and a blockquoted echo after each of the four boundary forms (`## heading`, thematic break, `Summary:`, `Unrelated summary`).\n\nI did not accept the tokenizer's correctness on faith. I ran an oracle over every probe body: render it with the same library in GFM mode, strip `<pre>`, `<code>` and `<blockquote>` subtrees, and ask whether the echo survives as visible prose. Across 37 hand-built shapes, 25 adversarial interaction shapes and a systematic sweep of 1680 generated bodies (seven list-marker forms × four pre-indents × tight/loose × twelve continuation indents including tabs × inside/outside a finding, plus a blockquote-after-list family), there is **no body that the guard accepts while the echo renders as unquoted visible text**. The corresponding spec rules — list content indent, the \"item starting with indented code\" case that makes `-` plus five spaces a real code block, and blockquote laziness — were checked by hand against CommonMark on the rows where the guard's answer changed.\n\nThe four section-bound mechanisms named in the acceptance are intact: `token.type === \"heading\" || token.type === \"hr\"` revokes at `:52`, the per-line ATX/known-heading/summary-label test at `:54-57` is unchanged bytes, the literal denylist at `:30-34` is unchanged, the refusal string at `:66` is byte-identical, and the 40 KiB gate in `scripts/post-review-comment.mjs:35-40` is untouched. `findingHeadings` and `findingIndex` are unchanged, so the live index gate is unaffected.\n\nRegression check on real traffic: both canonical initial reviews, the round-1 focused review, the current and prior PR bodies and the conductor receipt file — eleven real documents, 507 B to 26 KB — are accepted by OLD and by NEW alike. Removing inline-span permission does not refuse any real reviewer output in hand, and it does not contradict the documented escape, which names a Markdown blockquote inside an indexed finding (`.agentrig/skills/ship/SKILL.md:133`, `.agentrig/skills/topic/SKILL.md:259`).\n\n## Finding\n\n### LOW: The CommonJS require of ESM-only marked breaks the posting guard on the Node 22.0–22.11 range the repo declares supported\n\n`scripts/review-finding-index.mjs:10`\n\n`marked@18.0.11` is ESM-only: its `package.json` is `\"type\": \"module\"` and its export map offers a single `default` condition pointing at `lib/marked.esm.js`, with no `require` condition. `createRequire(...)('marked')` therefore depends on Node's unflagged `require(esm)` support, which arrived in Node 22.12.0. Every package here declares `engines.node: \">=22\"`.\n\nFailure scenario: a conductor on Node 22.0–22.11 loads `scripts/post-review-comment.mjs`, which imports this module at `scripts/post-review-comment.mjs:8`, and the import throws before any argument is parsed. The reviewer's verdict cannot be posted, and `packages/cli/test/post-review-comment.test.ts` and `packages/cli/test/review-finding-index.test.ts` fail at import for the same reason, so `pnpm test` is red on a Node version the manifests claim to support. Reproduced exactly on this machine by disabling the feature: `node --no-experimental-require-module -e \"import('scripts/review-finding-index.mjs')\"` gives `ERR_REQUIRE_ESM: require() of ES Module .../marked/lib/marked.esm.js from .../packages/cli/package.json not supported`.\n\nNonblocking under shipping policy §2: the failure is fail-closed and loud — nothing is posted, no echo is admitted, and hosted CI pins `node-version: 22` at `.github/workflows/ci.yml:27` and `:79`, which resolves to a current 22.x where the require succeeds. It is a portability defect, not a safety gate, and it costs one line to fix.\n\nProposed fix: keep the resolution through the declaring package but load the module as ESM, replacing the require with `await import(createRequire(new URL('../packages/cli/package.json', import.meta.url)).resolve('marked'))` at the top level of this ESM module; alternatively raise `engines.node` to `>=22.12` across the manifests so the declared floor matches the requirement.\n\n## Nonblocking observations (ledger advisories, no residual issues)\n\n**A blockquote's lazy continuation is now permitted, and two pre-existing negative assertions were relaxed to accommodate it.** `packages/cli/test/post-review-comment.test.ts:386` and `:439` both changed from appending the echo directly after a blockquoted citation to appending it after an extra newline. The reason is real: an unmarked line immediately after a `> ` line is a paragraph continuation of that blockquote under CommonMark laziness, so the tokenizer hands the whole thing back as one `blockquote` token and the guard skips it, where OLD refused. I confirmed the echo lands inside `<blockquote>` in the rendered output, so it displays as quotation rather than as a bare echo — the same rationale the ledger already accepted when it rebutted the unclosed-fence observation, and consistent with the acceptance clause that blockquotes carry permission. What is missing is a pin: no test in the delta records that this shape is now permitted, so a future repair could flip it silently in either direction. Worth one assertion, not an issue.\n\n**Extreme nesting inside the guard exhausts the stack instead of reaching the size gate.** `scripts/post-review-comment.mjs:33` calls the guard before the 40 KiB check at `:35`, which is the OLD ordering, but OLD did a linear line scan where NEW runs a full Markdown lexer. A body of roughly 2906 nested blockquote markers or 5813 nested emphasis runs — both well under 40 KiB — raises `RangeError: Maximum call stack size exceeded`. It is caught by the helper's outer handler, so the run exits 2 with nothing posted; the cost is a confusing message rather than an admitted echo, and no plausible verdict reaches that depth. A 1 MiB pathological body still completes in about 3.3 s, so there is no runaway. Moving the size check above the guard call would also make the diagnostic honest.\n\n**Residual risk I could not eliminate: the oracle and the guard are the same parser.** GitHub renders with cmark-gfm, not with this library, so a divergence between the two on what constitutes a code block or a blockquote would be invisible to my rendering oracle by construction. There is no second Markdown implementation anywhere in this dependency tree to cross-check against, and I did not send probe bodies to GitHub's rendering endpoint. I mitigated this by checking the changed rows against the CommonMark text directly, and the operator explicitly directed this tokenizer, so this is inherent to the chosen approach rather than a defect in the delta. Recording it so the closure is not read as stronger than it is.\n\n`docs/STATUS.md` now describes the guard accurately, including the inline-span reversal; the round-1 overstatement about lazy continuations that the previous focused review flagged is gone. Own-test polish from earlier rounds remains advisory.\n\n## Probes, mutants and final state\n\nAll reviewer-owned artifacts live under `/private/tmp/ship455-r2/review/probe` and `/private/tmp/ship455-r2/review`, outside Git ancestry, with OLD and NEW copies of the helper imported side by side.\n\n1. `probe.mjs` — 37 bodies, OLD versus NEW versus rendered visibility: assigned reproducer shapes, positive controls, negative controls, boundary revocation, and adversarial shapes (HTML block, table cell, setext heading, link reference definition, fence inside a list item, indented fence).\n2. `probe2.mjs` — 25 further bodies: CRLF variants, blockquote interruption and laziness families, list-container drop hunts, and attempts to make the finding-heading set disagree with the token stream via HTML blocks, tables and indented code.\n3. `sweep.mjs` — 1680 systematically generated list-continuation and quote-after-list bodies; zero accepted-and-visible.\n4. `corpus.mjs` — eleven real review comments, PR bodies and receipts through both helpers.\n5. `perf.mjs` — eight pathological bodies, timing and stack behaviour, plus a bisection of the overflow thresholds.\n6. Unmutated control before any mutant: `./node_modules/.bin/vitest run packages/cli/test/post-review-comment.test.ts packages/cli/test/review-finding-index.test.ts` — 2 files, 134 passed, exit 0.\n\nSix mutants, applied one at a time to the real `scripts/review-finding-index.mjs`, each restored from a pre-mutation copy and SHA-256 verified before the next; no runs overlapped. Every one is killed. The first four are the ones the PR body names:\n\n| Mutant | Result (all exit 1) |\n|---|---|\n| M-list-container-permission — skip list traversal while a finding is open | 4 failed / 130 passed |\n| M-inline-span-permission — restore inline-span masking in finding prose | 1 failed / 133 passed |\n| M-code-permission-removed — always echo-check code tokens | 9 failed / 125 passed |\n| M-quote-permission-removed — always echo-check blockquote tokens | 6 failed / 128 passed |\n| M-hr-heading-reset-removed — drop the token-level heading/break revocation | 4 failed / 130 passed |\n| M-permission-anywhere — grant citation permission outside findings too | 9 failed / 125 passed |\n\nThe fifth is mine and matters: the thematic-break regex that OLD applied per line is gone from the line test, so the `hr` token branch at `:52` is the only thing revoking permission at a `---` boundary. It is load-bearing and it is pinned.\n\nFail-first replayed rather than taken on trust: I swapped the OLD helper into place and ran `./node_modules/.bin/vitest run packages/cli/test/post-review-comment.test.ts -t \"R2-F3\"` — 4 failed, 2 passed, 73 skipped, exit 1, matching the PR body's claim. The two that pass are the genuine nested-code positive controls, which are regression pins rather than fail-first evidence. None of the new assertions is vacuous: each refusal case asserts the exit status, the exact refusal reason on stderr and that the stub `gh` was never invoked; each acceptance case asserts exit 0 and exact posted bytes.\n\nPR body claims I verified: the four named round-2 mutants are real and each is killed; the fail-first count for the round-2 tests; the repaired focused control of 134 passing tests across the two CLI files; the three-file scope with no product, dependency, skill or configuration change; that `marked` is resolved through its existing declaring workspace package rather than newly added; that section bounds, the literal denylist, the refusal text and the 40 KiB gate are unchanged. Claims I did not verify: the round-2 declared receipts and the CRLF pre-push run reported in the PR body (I used the conductor's exact-head receipts as my proof input), the reported timings, and hosted CI.\n\nFinal state: `git status --porcelain` empty, HEAD still `a45dc1f93db4a581371d0b94f0e7e518347fd890`, `scripts/review-finding-index.mjs` back to its committed bytes `84d6709b6d827a9410b374200ef47b34d0ceb15d96b07f507e0eb1ae12845ab8`, all probe subprocesses joined, no background jobs left. `review-base-455-r2` is still at `aaa64642228fa35ccd3d176dd40de3e7fd87ca49`; I created, modified and deleted no ref. I ran no declared check, bootstrap or preflight, edited no product code, and posted, pushed and merged nothing. The conductor's proof tree and the shared temporary root are left for the conductor.",
  "Review complete. Tree restored to the exact reviewed head with a clean tracked/index state, no background jobs outstanding.\n\n---\n\nReviewed head: a45dc1f93db4a581371d0b94f0e7e518347fd890\n\nVERDICT: PASS\n\nFocused delta review, repair round 2. OLD `aaa64642228fa35ccd3d176dd40de3e7fd87ca49` → NEW `a45dc1f93db4a581371d0b94f0e7e518347fd890` (single commit `a45dc1f`; OLD and `origin/main` `6e0ec1f5181d34209e3fc557acc6c29d10984594` both verified as ancestors of NEW via `git merge-base --is-ancestor`, and OLD ≠ NEW). I reviewed the OLD..NEW material delta and its direct interactions only. F1, F2 and F4 were not re-audited; unchanged code and nonblocking scope preferences were not reopened.\n\n**Result: assigned blocker F3 is closed. No blocking findings. Three LOW non-blocking advisories.**\n\n## Inputs verified before reviewing\n\nConductor receipts at `/private/tmp/ship455-r2/checks.md` name head `a45dc1f93db4a581371d0b94f0e7e518347fd890`, proof worktree `/Users/amit/ship455-r2-proof`, bootstrap `pnpm install --frozen-lockfile` exit 0 in both trees, then the declared steps in order — preflight 21:50:56Z–21:50:57Z, build 21:50:57Z–21:51:05Z, test 21:51:05Z–21:52:09Z (270 files passed / 1 skipped, 5299 tests passed / 6 skipped), typecheck 21:52:09Z–21:52:14Z — each exit 0 on 2026-09-20. The receipt head matches my review target. I ran no declared check, no bootstrap and no preflight; my TMPDIR is `/private/tmp/ship455-r2/review`, outside Git ancestry. I did not create, modify or delete `review-base-455-r2` or any other ref.\n\nI inspected the pre-existing reviewer probe artifacts in that TMPDIR before deciding what to reuse. `guard.orig.mjs` is byte-identical to the tree file (sha256 `84d6709b…`), so I reused it as the restore source. I did **not** reuse `probe/new-guard.mjs`: it differs from the tree file (its `createRequire` argument is an absolute string rather than the shipped `import.meta.url` form), so every disposition probe below imports the real tree file directly. I wrote and ran my own probe, mutant, oracle and perf scripts rather than replaying the prior run's outputs.\n\nDelta content: three files — `scripts/review-finding-index.mjs`, `packages/cli/test/post-review-comment.test.ts`, `docs/STATUS.md`. No product code, no events, no skills, no config, no `docs/ROADMAP.md`; no deviation record is required. `findingHeadings` and `post-review-comment.mjs` are untouched, so F1's live-index fix and the 40 KiB gate are unchanged by construction.\n\n## Assigned blocker disposition\n\n**F3 — CLOSED.** The assigned blocker was:\n\n> ### MEDIUM: A blank line inside a list item turns a lazy list paragraph back into a permitted citation, re-opening the F3 echo leak\n\n`scripts/review-finding-index.mjs:35-68` replaces the hand-rolled line scanner with `marked`'s `lexer`, resolved through the declaring workspace package. `packages/cli/package.json` declares `marked` at `18.0.11` as a regular dependency and `createRequire(new URL('../packages/cli/package.json', import.meta.url))` resolves it to `node_modules/.pnpm/marked@18.0.11/…`, so the operator's direction to tokenize through the existing declaring package is met rather than re-deriving CommonMark indentation.\n\nOnly `code` and `blockquote` block tokens are dropped from the echo scan, and only while `inFinding`; `list` tokens are recursed into item-by-item so item content is judged on its own token types. I confirmed the closure on the exact shapes the blocker named, plus variants it did not:\n\n| Shape (inside a finding section) | NEW |\n|---|---|\n| `-`/`*`/`+`/`1.`/`1)`/`10.` item, blank line, four-space echo | REFUSE (all 6 markers) |\n| same, tab-indented echo | REFUSE (all 6 markers) |\n| same, 3-space and 5-space echo | REFUSE (all 6 markers) |\n| bullet and ordered item, no blank line, four-space echo | REFUSE |\n| item, two blank lines, four-space echo | REFUSE |\n| nested `- outer` / `  - inner`, blank, six-space echo (under-indented for the inner item) | REFUSE |\n| `100.` item, blank, five-space echo | REFUSE |\n| real nested code: bullet item, blank, six- and eight-space echo | ACCEPT |\n| real nested code: `1.` item, blank, seven-space echo | ACCEPT |\n| fenced, tilde-fenced, top-level four-space, top-level tab, blockquote, wrapped blockquote | ACCEPT |\n| list item containing a real fence or a real blockquote | ACCEPT |\n| inline span, double-backtick span, wrapped inline span, inline span inside a list item | REFUSE |\n\nInline-code spans alone are echo-checked, per the explicit acceptance. Section bounds hold: ATX headings, `---`, `***`, `___`, `* * *`, `Summary:`, `**Summary**`, `Unrelated summary` and both setext underlines each revoke permission for a following fence or blockquote, while blank lines alone do not end the section and a second finding heading re-arms it. Nothing is permitted outside a finding: fenced, indented and blockquoted echoes before any finding heading all refuse. The refusal text is byte-identical (`reviewer body echoes instructions; not a verdict`), the literal denylist is unchanged, and whitespace reflow across LF, CRLF, tabs and runs of spaces still refuses for all three pinned sentences.\n\n**Fail-first, verified directly.** I copied the OLD (`aaa6464`) revision of the guard over the tree file and reran `packages/cli/test/post-review-comment.test.ts`: the four new `R2-F3` list-continuation and inline-span cases fail (`expected +0 to be 2`), 75 others pass. The new tests are not vacuous. I restored the file and confirmed the sha256 before continuing.\n\n## Rendering-oracle sweep\n\nThe strongest evidence I have for the closure is an automated leak oracle rather than enumerated cases. I generated 8316 bodies from the cross product of 4 openings × 9 boundary labels × 11 container contexts × 21 echo placements. For each body I took the guard's disposition, and for every ACCEPT I rendered the body with the same `marked@18.0.11` in GFM mode, stripped `<pre>`, `<code>` and `<blockquote>` subtrees, and searched the remainder for the normalized echo sentence.\n\n**660 bodies accepted, 0 leaks.** Every echo the guard admits renders inside a code or quotation element; none renders as ordinary prose. Against OLD, 180 bodies tightened and 96 relaxed, and all 96 relaxations are one family — CommonMark lazy continuation of an open blockquote — covered by the first advisory below.\n\n## Named mutants\n\nEach mutant was applied to the real tree file, run against `packages/cli/test/post-review-comment.test.ts` and `packages/cli/test/review-finding-index.test.ts` (134 tests), then restored and sha256-verified before the next; runs never overlapped.\n\n| Mutant | Result |\n|---|---|\n| `M-list-container-permission` — skip list items entirely while in a finding | killed, 4 failed |\n| `M-list-token-dropped` — drop list tokens from the scan | killed, 4 failed |\n| `M-inline-span-permission` — restore inline-span masking inside findings | killed, 1 failed |\n| `M-code-permission-anywhere` — permit code tokens regardless of section | killed, 2 failed |\n| `M-quote-permission-anywhere` — permit blockquote tokens regardless of section | killed, 7 failed |\n| `M-permission-anywhere` — permit both regardless of section | killed, 9 failed |\n| `M-hr-reset-removed` — drop the thematic-break reset | killed, 4 failed |\n| `M-no-reset` — drop both resets | killed, 4 failed |\n| `M-heading-rearm-removed` — never re-arm on a finding heading | killed, 15 failed |\n| `M-heading-reset-removed` — drop the heading reset, keep `hr` | **SURVIVED, 134 passed** |\n\nThe one survivor is the first advisory below. All ten restores verified; the unmutated control run is 179 passed across the four guard-related test files.\n\n## Findings\n\n### LOW: The heading-token section reset is load-bearing only for setext headings, and no test pins it\n\n`scripts/review-finding-index.mjs:52`\n\n`token.type === \"heading\"` is redundant for ATX headings — the per-line check at `:54` already re-evaluates `inFinding` for any line starting with `#`. It is load-bearing solely for setext headings, which carry no `#` and are not in the `headings` set. Deleting `heading` from that condition leaves all 134 targeted tests green, and I confirmed the resulting behavior change directly: a finding section followed by `Evidence` / `---`, by `Evidence` / `===`, or by prose immediately underlined with `---`, then a fenced echo, flips from REFUSE at NEW to ACCEPT under the mutant. HEAD is correct in all three; only the coverage is missing.\n\n**Failure scenario.** A later refactor that folds the reset into the per-line ATX check — a natural simplification, since the condition looks redundant — silently restores citation permission across a setext section boundary, and the suite stays green.\n\n**Fix.** Extend the existing `R1-F2 explicit section boundary revokes citation` parameterization at `packages/cli/test/post-review-comment.test.ts:487` with a setext case, for example a `Evidence\\n---` boundary before the blockquoted citation. Non-blocking: this is own-test coverage of behavior that is already correct at NEW, so it is an advisory, not a residual issue.\n\n### LOW: Lazy blockquote continuation gained citation permission, and two existing assertions were edited to accommodate it without pinning the new rule\n\n`scripts/review-finding-index.mjs:44-46`, with `packages/cli/test/post-review-comment.test.ts:386` and `:439`\n\nBoth edited assertions insert a `\"\\n\"` before the appended echo. The reason is a real OLD→NEW behavior change that the delta does not state: when an echo line directly follows a `>`-prefixed line with no blank line between them, CommonMark makes it a lazy continuation of that blockquote's paragraph. OLD's line scanner only dropped lines literally starting with `>`, so it refused; NEW hands the whole blockquote token to the exemption, so it accepts. This is the entire relaxed set in my 8316-body sweep — 96 bodies, all of the `lazy-quote` family.\n\nI judge the new behavior correct rather than a leak: I rendered it and the continuation lands inside `<blockquote>`, so the echo is displayed as a quotation, which is exactly what the acceptance permits. The problem is evidentiary. The delta relaxed two negative assertions to match a behavior change, and no test now distinguishes \"echo lazily continuing a quote\" (permitted) from \"echo as a fresh paragraph after a quote\" (refused) — I verified the latter still refuses.\n\n**Failure scenario.** A future change to the blockquote branch widens the exemption past the rendered quote boundary; the two edited assertions no longer detect it because their inputs were moved off that boundary, and nothing else covers it.\n\n**Fix.** Add a paired case near `packages/cli/test/post-review-comment.test.ts:470`: a quote line followed immediately by the echo posts, and the same quote line followed by a blank line and the echo refuses. Non-blocking own-test advisory.\n\n### LOW: Pathologically nested Markdown makes the guard throw a non-canonical error instead of its refusal\n\n`scripts/review-finding-index.mjs:62`\n\n`lexer(body)` runs before the 40 KiB gate in `scripts/post-review-comment.mjs:35`, and `marked`'s recursive descent overflows the stack on deeply nested input that fits well inside that budget. A 40 KiB body of `'> '` repeated 20000 times throws `Maximum call stack size exceeded` in 85 ms; 20000 nested emphasis runs do the same in 424 ms. OLD's line scanner could not throw here.\n\nThe failure direction is safe, which is why this is LOW. I ran the real helper end to end on the deep-quote body: exit 2, no `gh` invocation, no receipt written. Throughput is otherwise fine — 1 MiB of prose lexes in 34 ms, 1 MiB of blank lines in 137 ms, 40 KiB of nested lists in 11 ms, and nesting depth 1600 parses normally — so this is not a denial-of-service concern, only an operator-facing one.\n\n**Failure scenario.** A reviewer body that trips the limit refuses with a V8 runtime message rather than the contract's refusal text, so an operator reading stderr cannot tell a guard refusal from a crash, and at extreme sizes the process aborts on heap exhaustion before any receipt exists.\n\n**Fix.** Wrap the `lexer(body)` call so a parse failure is rethrown as the canonical refusal, or move the 40 KiB gate ahead of `assertReviewerVerdict`. Non-blocking and deferrable.\n\n## PR body claims\n\nVerified for this delta: `marked` is resolved through its existing declaring CLI workspace package via `createRequire`; only blockquote and fenced/indented code block tokens confer citation permission within a finding section; inline code including wrapped spans is now echo-checked; bullet and ordered list continuations including blank-line plus four-space and tab variants remain prose while real nested code blocks pass; heading and thematic-break section bounds, the literal denylist, the exact refusal string and the 40 KiB gate are unchanged. The `docs/STATUS.md` edit is accurate, including the previously overstated sentence about lazy continuations, which now holds for both the tight and blank-line-separated list forms.\n\nNot verified, and not in this delta's scope: the round-0 historical receipts, mutant table and CRLF pre-push proof in the PR body; the #450, #451 and #454 claims; the round-1 F1/F2 closures; and hosted exact-head CI, which is a landing gate outside a reviewer's remit. I accepted the conductor's same-head receipts as the declared-check evidence and ran no full suite. One secondary observation I did not raise as a finding: a `Summary:` label written as a lazy blockquote continuation no longer revokes permission for a following fence, where OLD did — I consider this correct, since a line rendered inside a quote is not a document-level boundary, and heading and thematic-break bounds still revoke through the same construct.\n\n## Probes run and final state\n\nTargeted invocations only, via `node_modules/.bin/vitest` directly: the four guard-related files (`post-review-comment`, `review-finding-index`, `reviewer-verdict-instruction`, `reviewer-residuals-instruction-contract`) at HEAD, 179 passed; one fail-first run with the OLD guard restored; ten mutant runs over two files; an 87-case disposition matrix; an 8316-body rendering-oracle sweep; an OLD-versus-NEW differential on the two edited assertions and on blockquote boundary interactions; a nine-shape parse-cost measurement; a depth ladder; and one end-to-end helper run with a stubbed `gh`.\n\nFinal state: `HEAD` is `a45dc1f93db4a581371d0b94f0e7e518347fd890`, matching the recorded value; `git status --porcelain -uall` is empty; `git diff` and `git diff --cached` are empty; `scripts/review-finding-index.mjs` is back to sha256 `84d6709b…`. Every mutation byte is restored, all subprocesses joined, no background jobs. My probe scripts remain in my own TMPDIR as evidence; I removed my scratch directories and touched no conductor resource, no shared ref, no sibling tree and no permission. I pushed nothing, posted nothing and merged nothing — this verdict goes to the conductor."
];
